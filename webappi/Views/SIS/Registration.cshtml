@{
    ViewData["Title"] = "Student Registration";
}

<div class="card shadow-sm border-0">
    <div class="card-header bg-transparent border-bottom">
        <h5 class="mb-0">Student Registration</h5>
    </div>
    <div class="card-body">
        <p class="text-muted">Registration form will go here.</p>
        <!-- Placeholder for future form -->
        <form>
            <div class="mb-3">
                <label class="form-label">Student Name</label>
                <input type="text" class="form-control" disabled placeholder="Coming soon..." />
            </div>
            <button type="button" class="btn btn-primary" disabled>Register</button>
        </form>
    </div>
</div>
